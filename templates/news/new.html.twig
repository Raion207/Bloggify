{% extends 'layout/base.html.twig' %}

{% block body %}

    <br>
    <div class="container">
        <h1>News Creation</h1>

        {{ include('news/_form.html.twig') }}

        <a href="{{ path('app_news_index') }}" class="btn btn-outline-secondary" style="float: right; margin-top: -38px">Back to list</a>
    </div>
    <br>

{% endblock %}
