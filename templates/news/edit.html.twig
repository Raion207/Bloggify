{% extends 'layout/base.html.twig' %}

{% block body %}

    <br>
    <div class="container">
        <h1>News Edition</h1>

        {{ include('news/_form.html.twig', {'button_label': 'Update'}) }}

        <a href="{{ path('app_news_index') }}" class="btn btn-outline-secondary" style="float: right; margin-top: -38px; margin-right: 975px">Back to list</a>

        {{ include('news/_delete_form.html.twig') }}
    </div>
    <br>

{% endblock %}
